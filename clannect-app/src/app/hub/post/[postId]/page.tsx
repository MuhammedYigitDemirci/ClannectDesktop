'use client'

import HubPage from '../../page'

// This page handles /hub/post/[postId] URLs
// It renders the same HubPage component which will automatically scroll to the post
export default function HubPostPage() {
  return <HubPage />
}
